{
  "name": "b35-instagram-archive-importer",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "lint:js": "eslint js/",
    "lint:php": "vendor/bin/phpcs --standard=phpcs.xml.dist",
    "test:php": "vendor/bin/phpunit",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.js": "eslint",
    "*.php": "vendor/bin/phpcs --standard=phpcs.xml.dist"
  },
  "devDependencies": {
    "@wordpress/eslint-plugin": "^22.0.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0"
  }
}
